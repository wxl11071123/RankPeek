# RankPeek Website

This directory contains the standalone RankPeek official website draft.

The previous generated page has been cleared. The app is intentionally a blank Vue shell so the next version can be designed from scratch.

## Commands

```bash
npm install
npm run dev
npm run build
```

The Vite dev server uses port `5174`.
